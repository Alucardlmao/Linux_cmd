chmod - Change the permissions {u: User/owner permissions}
                               {g: Group permissions}
                               {o: Other permissions}
                               {+: Add permission}
                               {–: Remove permissions}
                               {=: Set permissions explicitly}

chown - Change file ownership

chgrp - Change group ownership

umask - Set default file permissions
